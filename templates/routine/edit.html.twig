{% extends 'base.html.twig' %}

{% block title %}Bloom - Edit Routine{% endblock %}

{% block body %}
    <div>
        <h1>Edit Routine : {{ workoutPlan.name }}</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Update Routine</button>
        {{ form_end(form) }}

        <br>
        <a href="{{ path('app_workout_routine') }}">Back to list</a>
    </div>
{% endblock %}
